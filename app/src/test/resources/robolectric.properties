# compileSdk is 36, newer than any android-all image Robolectric ships (it maxes at 35). Pin the
# Robolectric runtime to a stable, well-supported level so tests don't fail resolving a 36 image.
sdk=34
