<?xml version="1.0" encoding="utf-8"?>
<!-- System widget-picker preview (API 31+) for the Scan Receipt widget. Static sample.
     Inflated as RemoteViews, so only @RemoteView view types are allowed (no <Space>/<View>). -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/widget_card_bg"
    android:gravity="center_vertical"
    android:padding="18dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <ImageView
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:background="@drawable/widget_chip_brand"
            android:src="@drawable/widget_ic_camera"
            android:scaleType="centerInside"
            android:padding="14dp"
            android:tint="@color/widget_on_brand"
            android:contentDescription="@null" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="16dp"
            android:orientation="vertical">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/widget_scan"
                android:textColor="@color/widget_on_surface"
                android:textStyle="bold"
                android:textSize="18sp" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="3dp"
                android:text="@string/widget_scan_body"
                android:textColor="@color/widget_on_surface_variant"
                android:textSize="12sp" />
        </LinearLayout>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="10dp"
            android:background="@drawable/widget_chip_brand"
            android:paddingHorizontal="18dp"
            android:paddingVertical="9dp"
            android:text="@string/widget_scan_action"
            android:textColor="@color/widget_on_brand"
            android:textStyle="bold"
            android:textSize="13sp" />
    </LinearLayout>
</LinearLayout>
